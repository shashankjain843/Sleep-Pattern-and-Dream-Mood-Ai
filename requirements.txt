# requirements.txt — pinned for reproducible installs
# Python 3.14 notes:
#   - tensorflow and pyedflib do not yet have Python 3.14 wheels;
#     they are conditionally installed only on Python < 3.13.
#   - All other packages below have Python 3.14 wheels on PyPI.

# Core numerics
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0

# Signal processing / EEG
mne==1.12.1
pyedflib; python_version < "3.13"

# Deep learning (sleep model)
tensorflow; python_version < "3.13"
tensorflow-cpu; python_version < "3.13"

# Machine learning (mood model)
scikit-learn==1.9.0
xgboost==3.3.0
joblib==1.5.3

# Web framework
fastapi==0.139.0
uvicorn==0.49.0
python-multipart==0.0.32
starlette==1.3.1

# Frontend
streamlit==1.58.0
plotly==6.8.0
matplotlib==3.11.0
seaborn==0.13.2

# HTTP client
requests==2.32.3

# PDF export
reportlab==4.2.5

# Authentication (pure Python — Python 3.14 compatible)
PyJWT==2.9.0

# LLM integration (optional — set ANTHROPIC_API_KEY to enable)
anthropic==0.30.0

# Testing
pytest==8.3.4
httpx==0.27.2
